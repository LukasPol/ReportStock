<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <title>StockReport</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <div class="content z-0 flex flex-col h-screen items-center bg-gray-200">
      <div class="z-0 w-4/5 mt-20 px-20 pt-6 pb-8 overflow-hidden rounded-md flex flex-col items-center transition bg-white transform duration-600 ease-in-out">
        <%= yield %>
      </div>
    </div>
  </body>
</html>
